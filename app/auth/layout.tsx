import { ReactNode } from 'react'

export const metadata = {
  title: 'تسجيل الدخول | St. Kriakos Org'
}

export default function AuthLayout({ children }: { children: ReactNode }) {
  return <>{children}</>
}
